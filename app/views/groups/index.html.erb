<div class="container">
  <div class="jumbotron">
    <p class="lead", class="text-center", style="font-family:sans-serif">
      <strong style="font-family:Chewy;color:#3A92C2"> Who Da Best at FIFA?</strong> is the website that all FIFA lovers
      have been waiting for. No more arguments, no more bragging, no more speculating on who's the best player amongst your friends... Simply join or create a group with your friends and we'll take care of finding who the best player is! Just play your favorite game, send your score and opponent's name by text and we'll compile all your information through a unique scoring system.<em> PS: FIFA banter is still highly encouraged... </em>
    </p>
  </div>

  <div class="text-center">
   <span class="glyphicon glyphicon-flash"> <%= link_to "C R E A T E    G R O U P" , new_group_path, class:"btn btn-lg btn-primary"%></span>  <span class="glyphicon glyphicon-flash"></span>
  </div>


  <h1 class="text-center">List of All Groups</h1>
  <ul>
    <% @groups_joined.each do |group| %>
      <li><strong> <%= group.name %></strong> ( <%= group.location %> )
          <a href="#" class="btn btn-xs btn-warning">VIEW YOUR GROUP</a>
    <% end %>

    <% @groups_not_joined.each do |group| %>
      <li><strong> <%= group.name %></strong> ( <%= group.location %> )
      <%= button_to "JOIN" , memberships_path(group_id: group), class:"btn btn-xs btn-primary"%><span class="glyphicon glyphicon-arrow-right"></span> </li>
    <% end %>
  </ul>

</div>
